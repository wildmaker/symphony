# Agent Instructions

<!-- repo-context-bootstrap:BEGIN -->
Canonical agent context lives in `.agents/`.

Start with:

- `.agents/instructions.md`
- `.agents/profile.md`
- `.agents/WORKFLOW.md`

Use tool-specific files only as adapters. Do not duplicate long-lived project rules across root, `.github`, and `.cursor`.
<!-- repo-context-bootstrap:END -->
